'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import Image from 'next/image';
import { Card } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Button } from '@/components/ui/button';
export default function Login(){const [login,setLogin]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const r=useRouter();
async function submit(e:React.FormEvent){e.preventDefault(); const res=await fetch('/api/auth/login',{method:'POST',body:JSON.stringify({login,password})}); if(!res.ok){setError('Ошибка авторизации'); return;} const data=await res.json(); r.push(data.role==='ADMIN'?'/admin':'/employee');}
return <div className='min-h-screen flex items-center justify-center p-4'><Card className='w-full max-w-md space-y-4'><div className='text-center space-y-2'>{<Image src='/logo.png' alt='OFFONIKA' width={80} height={80} className='mx-auto'/>}<h1 className='text-2xl font-bold'>OFFONIKA</h1><p className='text-sm text-gray-500'>Система тестирования сотрудников</p></div><form onSubmit={submit} className='space-y-3'><Input placeholder='Логин' value={login} onChange={e=>setLogin(e.target.value)}/><Input placeholder='Пароль' type='password' value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className='text-sm text-red-500'>{error}</p>}<Button className='w-full'>Войти</Button></form></Card></div>}
